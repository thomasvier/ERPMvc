@model ERPWeb.Models.Administracao.Perfil

@{
    ViewBag.Title = "Excluir Perfil";
}

<h2>Excluir</h2>

<h3>Tem certeza que deseja excluir este perfil?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Ativo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Ativo)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Excluir" class="btn btn-default" /> |
            <button type="button" class="btn btn-default" onclick="location.href='@Url.Action("Index", "Perfil")'"><i class="glyphicon glyphicon-backward"></i></button>
        </div>
    }
</div>
